services:
  - type: web
    name: jmc-placement-portal
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    region: ohio
    envVars:
      - key: PORT
        value: 10000
